// Specify it is normal and spec mapped
renderhint NormalAndSpecMapped
 
// Define textures
texture0 MinDawnPol_d // Diffuse
texture1 MinDawnPol_n // Normal
texture2 MinDawnPol_s // Specular
texture3 MinDawnPol_r // Roughness
texture4 null // Height
texture5 null // Illumination

