// Specify it is normal and spec mapped
renderhint NormalAndSpecMapped
 
// Define textures
texture0 LeaBlaHid_d // Diffuse
texture1 LeaBlaHid_n // Normal
texture2 LeaBlaHid_s // Specular
texture3 LeaBlaHid_r // Roughness
texture4 null // Height
texture5 null // Illumination

